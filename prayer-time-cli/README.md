🕌 MH7Q Prayer Time CLI

أداة قوية تعمل عبر سطر الأوامر (Terminal) لجلب أوقات الصلاة بدقة عالية باستخدام تقويم أم القرى. صُممت لتكون سريعة، خفيفة، وسهلة الاستخدام في بيئات التيرمينال المختلفة.
🚀 المميزات

    ذاكرة ذكية للموقع: تقوم الأداة بحفظ مدينتك تلقائياً بعد أول استخدام، فلا داعي لإدخالها كل مرة.

    تنسيق 12 ساعة: عرض الوقت بنظام صباحاً/مساءً (مثال: 05:30 PM) بدلاً من نظام 24 ساعة.

    واجهة احترافية: مخرجات ملونة ومنظمة ومدعومة بفن ASCII.

    دعم ثنائي اللغة: تدعم اللغة العربية والإنجليزية بالكامل.

    جدول شهري: إمكانية استخراج جدول مواقيت الصلاة لشهر كامل وحفظه في ملف نصي.

📦 التثبيت (Installation)

    نسخ المستودع (Clone):
    Bash

git clone https://github.com/YOUR_USERNAME/prayer-time-cli.git
cd mh7q-prayer-cli

تثبيت المتطلبات:
Bash

    pip install -r requirements.txt

⚡ طريقة الاستخدام (Usage)

التشغيل الأساسي (الوضع التفاعلي):
Bash

python3 prayer_times.py

سيطلب منك البرنامج تحديد اللغة والموقع في أول مرة فقط.

تغيير اللغة يدوياً:
Bash

python3 prayer_times.py --lang ar

استخراج جدول للشهر الحالي:
Bash

python3 prayer_times.py --month

إعادة ضبط الإعدادات (حذف الموقع المحفوظ):
Bash

python3 prayer_times.py --reset

🛠️ الإعدادات

تقوم الأداة بإنشاء ملف محلي باسم config.json لحفظ تفضيلاتك (المدينة واللغة). لإعادة ضبط الأداة، يمكنك ببساطة حذف هذا الملف أو استخدام الأمر --reset.
📜 الترخيص (License)

هذا المشروع مرخص بموجب MIT License.

صُنع بواسطة MH7Q 🇸🇦
